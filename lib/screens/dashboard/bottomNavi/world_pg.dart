import 'package:flutter/material.dart';

class world_pg extends StatefulWidget {
  const world_pg({Key? key}) : super(key: key);

  @override
  State<world_pg> createState() => _world_pgState();
}

class _world_pgState extends State<world_pg> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('byeeee')),
    );
  }
}
